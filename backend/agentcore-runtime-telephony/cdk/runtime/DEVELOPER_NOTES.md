# Developer Notes — Telephony Agent Runtime Stack

Self-contained CDK app. Receives 7 CfnParameters from upstream `cdk-outputs/*.json` files via `scripts/deploy-all.sh` `--parameters`. Emits outputs WITHOUT `exportName`.

Placeholder only — see task 5 in `.kiro/specs/telephony-voice-ordering-agent/tasks.md`.
